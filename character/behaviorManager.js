class BehaviorDamageManager{
  update(objsArr){
    let arrWhoHasDamage = arr.filter(item => item.damage == true);
    let arrWhoHasHealth = arr.filter(item => item.health == true);
    arr.CharacterDamageController()
  };
};
